@using Service.ESS.Provider;
@using Service.ESS.Model;
@{
    BatteryService BatteryService = new BatteryService();
    Battery battery = BatteryService.ReadNow();
    string Info1 = "", Info2 = "", Info3 = "", Info4 = "", Info5 = "", Info6 = "", Info7 = "";

    if ( battery!=null)
    {
        Info1 = battery.updateTime.AddHours(8).ToString();
        Info2 = string.Format("{0:#,0.00}", battery.voltage);
        Info3 = string.Format("{0:#,0.00}", battery.charging_current);
        Info4 = string.Format("{0:#,0.00}", battery.discharging_current);
        Info5 = string.Format("{0:#,0.00}", battery.SOC);
        Info6 = string.Format("{0:#,0.00}", battery.Cycle);
        int cd = Convert.ToInt32(battery.charge_direction);
        Info7 = (cd == 1) ? "充電" : (cd == 2) ? "放電": "離線"  ;
    }
}
<div class="row">
    <!--lift-->
    <div class="col-2 h6">
        <div class="row " id="pc0">
            <div class="col font-weight-bold small">資料時間(UTC+8)</div>
            <div class="col font-italic">@Info1</div>
            <div class="w-100"></div>
            <div class="col font-weight-bold small">電池電壓(V)</div>
            <div class="col font-italic">@Info2</div>
            <div class="w-100"></div>
            <div class="col font-weight-bold small">充電電流(A)</div>
            <div class="col font-italic">@Info3</div>
            <div class="w-100"></div>
            <div class="col font-weight-bold small">放電電流(A)</div>
            <div class="col font-italic">@Info4</div>
            <div class="w-100"></div>
            <div class="col font-weight-bold small">電池容量(%)</div>
            <div class="col font-italic">@Info5</div>
            <div class="w-100"></div>
            <div class="col  font-weight-bold small">充電次數(次)</div>
            <div class="col font-italic">@Info6</div>
            <div class="w-100"></div>
            <div class="col font-weight-bold small">充電方向</div>
            <div class="col font-italic">@Info7</div>
        </div>
    </div>
    <!--middle-->
    <div class="col-8">
        <div id="BatteryChart" style="height:500px">
            <script type="text/javascript">
                var dom = document.getElementById("BatteryChart");
                var BatteryChart = echarts.init(dom);
                //時間兩位
                function paddingLeft(str, lenght) {
                    if (str.length >= lenght)
                        return str;
                    else
                        return paddingLeft("0" + str, lenght);
                }
                //X軸座標
                var xData = [];
                var h=@TempData["Batteryhh"];
                var m =@TempData["Batterymm"];
                for (var i = 0; i < 24; i++) {
                    for (var j = 0; j < 60; j = j + 15) {
                        var hh = i;
                        var mm = j;
                        if (h == 0) {
                            if (m > 0) {
                                mm = j + m;
                                if (mm >= 60) {
                                    mm = mm - 60;
                                    hh = hh + 1;
                                }
                            }
                        }
                        if (h > 0) {
                            hh = i + h;
                            if (m > 0) {
                                mm = j + m;
                                if (mm >= 60) {
                                    mm = mm - 60;
                                    hh = hh + 1;
                                }
                            }
                            if (hh >= 24) {
                                hh = hh - 24;
                            }
                        }
                        xData.push("'" + paddingLeft(hh.toString(), 2) + "時" + paddingLeft(mm.toString(), 2) + "分'");
                    }
                }
                //Y軸座標
                var BatteryData = [];
                BatteryData.push(@TempData["BatteryData"]);
                option = null;
                option = {
                    title: {
                        text: '每日用電瓦數',
                        textStyle: {
                            fontWeight: 'normal',
                            color: 'white'
                        },
                        x: "center"
                    },
                    tooltip: {
                        show: true
                    },
                    xAxis: [
                        {
                            name: '時間軸',
                            type: 'category',
                            data: xData,
                            axisLine: {
                                lineStyle: {
                                    color: 'white',
                                    width: 10
                                }
                            },
                            axisLabel: {
                                formatter: '{value}'
                            }
                        },
                    ],
                    yAxis: [
                        {
                            name: '用電單位',
                            type: 'value',
                            axisLine: {
                                lineStyle: {
                                    color: 'white',
                                    width: 2
                                }
                            },
                            axisLabel: {
                                formatter: '{value} kWh'
                            }
                        }
                    ],
                    series: [
                        {
                            "name": "每日用電瓦數",
                            "type": "bar",
                            "data": BatteryData,
                        }
                    ]
                };
                if (option && typeof option === "object") {
                    BatteryChart.setOption(option, true);
                }
            </script>
        </div>
    </div>
    <!--right-->
    <div class="col-2 h3">
        <div class=" bg-light">可用總電量</div>
        <div class="font-italic">@ViewBag.Demand  kWh</div>
        <div class="bg-light">可用電時數</div>
        <div class="font-italic">@ViewBag.RemainTime 小時</div>
    </div>
</div>





