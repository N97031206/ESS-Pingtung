@using Support.Authorize;

<div class="row text-center justify-content-center ">
    <div class="col-sm col-md col-lg">
        <div class="btn-group mr-2" role="group" aria-label="First group">
            <button type="button" class="@TempData["ButtomIndex"]" onclick="location.href='@Url.Action("Index", "Tab")'">@Resources.Resource.Index</button>
            <button type="button" class="@TempData["ButtomBulletin"]" onclick="location.href='@Url.Action("Bulletin", "Tab")'">@Resources.Resource.Bulletin</button>
            <button type="button" class="@TempData["ButtomInfo"]" onclick="location.href='@Url.Action("Info", "Tab")'">@Resources.Resource.Info</button>
            <button type="button" class="@TempData["ButtomAbnormal"] " onclick="location.href='@Url.Action("Abnormal", "Tab")'">@Resources.Resource.Abnormal</button>
            <button type="button" class="@TempData["Buttomhistory"]" onclick="location.href='@Url.Action("History", "Tab")'">@Resources.Resource.History</button>
            @if (!User.Identity.Name.Split(',')[2].ToString().Trim().Equals(RoleType.參觀帳號.ToString().Trim()))
            {
                <button type="button" class="@TempData["ButtomMaintain"]" onclick="location.href='@Url.Action("Maintain", "Tab")'">@Resources.Resource.Maintain</button>
            }
            <button type="button" class="@TempData["ButtomQRCode"]" onclick="location.href='@Url.Action("QRCode", "Tab")'">APP下載</button>
        </div>
    </div>
</div>

